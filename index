<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menù Elegante</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts for Inter and Playfair Display -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f8f8; /* Light background */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 1rem; /* Overall padding for body, adapts */
            box-sizing: border-box;
            overflow-x: hidden; /* Prevent horizontal scrolling */
        }

        .menu-wrapper {
            /* This div helps to constrain the overall size on large screens */
            max-width: 800px; /* Max width similar to A4 portrait */
            width: 100%; /* Take full width on smaller screens */
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15); /* Shadow for the whole card */
            border-radius: 1.5rem; /* Rounded corners for the wrapper */
            position: relative;
            perspective: 1000px; /* Crucial for 3D effect */
            height: 562px; /* Fixed height for desktop to match A4 half-page */
            background-color: transparent; /* Wrapper itself is transparent */
            overflow: hidden; /* Important for clean borders */
        }

        /* Responsive adjustments for smaller screens */
        @media (max-width: 800px) {
            .menu-wrapper {
                border-radius: 1rem;
                height: 80vh; /* Use vh for mobile height, ensures it fits screen */
                min-height: auto; /* Remove min-height to allow flexibility */
                max-height: none; /* No max height */
            }
            .cover-text {
                font-size: 4rem; /* Adjust font size for smaller screens */
            }
            .menu-title-main {
                font-size: 2rem; /* Adjust title for smaller screens */
            }
            .menu-item-title {
                font-size: 1.1rem; /* Adjust font size for smaller screens */
            }
            .menu-item-description {
                font-size: 0.8rem; /* Adjust font size for smaller screens */
            }
            .page-content {
                padding: 1rem; /* Reduced padding for page content on smaller screens */
            }
            .button-style {
                padding: 0.5rem 1.5rem; /* Smaller buttons on mobile */
                font-size: 0.8rem;
            }
            .menu-item-container {
                 margin-bottom: 0.8rem; /* Less space between items on mobile */
            }
            .divider {
                margin: 0.6rem auto; /* Smaller margin for dividers on mobile */
            }
        }

        /* The actual flipper element */
        .menu-card-flipper {
            width: 100%;
            height: 100%;
            position: relative;
            transform-style: preserve-3d;
            transition: transform 0.8s ease-in-out;
        }

        .menu-card-flipper.flipped {
            transform: rotateY(180deg); /* Flip horizontally */
        }

        .page-content {
            position: absolute;
            width: 100%;
            height: 100%; /* Occupy 100% of the flipper's height */
            -webkit-backface-visibility: hidden; /* Hide back of the element when flipped */
            backface-visibility: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center; /* Center content vertically */
            align-items: center;
            padding: 3rem; /* Consistent padding for both sides */
            box-sizing: border-box;
            border-radius: 1.5rem;
            background-color: white; /* Each page has its own white background */
            overflow-y: auto; /* Allow scrolling if content overflows vertically */
            -webkit-overflow-scrolling: touch; /* Smooth scrolling on iOS */
        }

        .front-page {
            /* No initial transform needed, default 0deg */
        }

        .back-page {
            transform: rotateY(180deg); /* Initial state for back page */
        }

        .cover-text {
            font-family: 'Playfair Display', serif;
            font-size: 8rem; /* Slightly larger */
            font-weight: 700;
            color: #2a2a2a; /* Darker, richer black */
            letter-spacing: 0.18em; /* More spacing */
            text-transform: uppercase;
            text-align: center;
            padding: 1rem;
            line-height: 1;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.05); /* Subtle text shadow */
        }

        .cover-gradient-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(255,255,255,0.8) 0%, rgba(240,240,240,0.8) 100%);
            border-radius: 1.5rem;
            z-index: -1;
        }

        .menu-title-main {
            font-family: 'Playfair Display', serif;
            font-size: 3rem; /* Larger for "La Nostra Proposta" */
            font-weight: 700;
            color: #3a3a3a;
            margin-bottom: 2rem; /* More space */
            text-align: center;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.03);
        }

        .menu-item-container {
            width: 100%;
            max-width: 450px;
            margin-bottom: 1.5rem; /* More space between items */
            flex-shrink: 0; /* Prevent shrinking if content is too large */
        }

        .menu-item-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem; /* Slightly larger */
            font-weight: 700;
            color: #444;
            margin-bottom: 0.3rem; /* Less space to description */
            text-align: center;
        }

        .menu-item-description {
            font-family: 'Inter', sans-serif;
            font-size: 1rem; /* Slightly larger */
            color: #555;
            line-height: 1.6;
            text-align: center;
        }

        /* Divider style */
        .divider {
            width: 80px;
            height: 2px;
            background-color: #d4d4d4;
            margin: 1rem auto;
            border-radius: 1px;
        }

        .button-style {
            padding: 0.9rem 2.5rem;
            background-color: #4a4a4a; /* Darker button */
            color: white;
            font-weight: 600;
            border-radius: 9999px; /* Fully rounded */
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease-in-out;
            cursor: pointer;
            border: none;
            outline: none;
        }
        .button-style:hover {
            background-color: #3a3a3a;
            transform: translateY(-2px); /* Slight lift effect */
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.25);
        }
    </style>
</head>
<body class="antialiased">
    <!-- Main container for the menu card, handles perspective and overall size -->
    <div class="menu-wrapper">
        <!-- Flipper element, handles the 3D rotation -->
        <div id="menuCardFlipper" class="menu-card-flipper">
            <!-- Front Page (Cover) -->
            <div id="frontPage" class="page-content front-page">
                <div class="cover-gradient-overlay"></div>
                <h1 class="cover-text">Menù</h1>
                <button id="flipToMenuBtn" class="button-style mt-10">
                    Scopri il Menù
                </button>
            </div>

            <!-- Back Page (Menu Content) -->
            <div id="backPage" class="page-content back-page">
                <h2 class="menu-title-main">La Nostra Proposta</h2>

                <!-- Antipasto -->
                <div class="menu-item-container">
                    <h3 class="menu-item-title">Antipasto</h3>
                    <p class="menu-item-description">Coni di sfoglia ripieni di mousse di mortadella, ricotta e pistacchio.</p>
                </div>
                <div class="divider"></div>

                <!-- Prima Portata -->
                <div class="menu-item-container">
                    <h3 class="menu-item-title">Prima Portata</h3>
                    <p class="menu-item-description">Gnocchi gluten-free al pomodoro con ricotta, basilico e olio EVO.</p>
                </div>
                <div class="divider"></div>

                <!-- Seconda Portata -->
                <div class="menu-item-container">
                    <h3 class="menu-item-title">Seconda Portata</h3>
                    <p class="menu-item-description">Filetto di manzo al coltello con uovo pastorizzato, olio EVO e crumble di cipolla fritta.</p>
                </div>
                <div class="divider"></div>

                <!-- Pre-Dessert -->
                <div class="menu-item-container">
                    <h3 class="menu-item-title">Pre-Dessert</h3>
                    <p class="menu-item-description">Sorbetto al mango e yogurt bianco con coulis di lampone e lime.</p>
                </div>

                <button id="flipToCoverBtn" class="button-style mt-8">
                    Torna alla Copertina
                </button>
            </div>
        </div>
    </div>

    <script>
        const menuCardFlipper = document.getElementById('menuCardFlipper');
        const flipToMenuBtn = document.getElementById('flipToMenuBtn');
        const flipToCoverBtn = document.getElementById('flipToCoverBtn');

        // Function to flip the menu card
        function flipCard() {
            menuCardFlipper.classList.toggle('flipped');
        }

        // Event listeners for flipping
        flipToMenuBtn.addEventListener('click', flipCard);
        flipToCoverBtn.addEventListener('click', flipCard);

        // Keyboard navigation for accessibility
        document.addEventListener('keydown', (event) => {
            if (event.key === 'ArrowRight' || event.key === ' ' || event.key === 'Enter') {
                if (!menuCardFlipper.classList.contains('flipped')) {
                    flipCard();
                }
            } else if (event.key === 'ArrowLeft') {
                if (menuCardFlipper.classList.contains('flipped')) {
                    flipCard();
                }
            }
        });
    </script>
</body>
</html>
